#include <stdio.h>
/**
 * main - print lowercase alphabets in reverse
 * Return: 0 (success)
 */
int main(void)
{
	char alphabet;
	
	for (alpahbet = 'z'; alphabet >= 'a'; alphabet--);
	{
		putchar(alphabet);
	}
	putchar('\n');
	return (0);
}

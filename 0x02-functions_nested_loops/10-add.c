#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a1: first number computed
 * @a2: second number computed
 * Return: 0 (success)
 */
int add(int a1, int a2)
{
	return (a1 + a2)
}
